# 呼び出し

自動呼び出しは 5 分間隔の schedule だけである。手動呼び出しは任意である。handler は payload の内容を解釈せず、すべての呼び出しで full reconcile を実行する。

非同期配送では、throttling または retry の枯渇後に invocation が破棄される場合がある。次に成功した定期 invocation が現在の設定を処理するため、通常の追加遅延は 5 分の間隔と 1 サイクルの実行時間の合計以下になる。失敗が継続する場合、収束時間の上限はない。
